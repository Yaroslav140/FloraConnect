<UserControl x:Class="FlowerShop.WpfClient.Views.Forms.OrderDetailsFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Order Id" Margin="0,0,0,6" Foreground="{StaticResource FgMuted}"/>
                    <TextBlock Text="{Binding OrderId}" Foreground="{StaticResource FgPrimary}"/>
                </StackPanel>
                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Дата" Margin="0,0,0,6" Foreground="{StaticResource FgMuted}"/>
                    <TextBlock Text="{Binding Date, StringFormat='dd.MM.yyyy'}" Foreground="{StaticResource FgPrimary}"/>
                </StackPanel>
            </StackPanel>

            <!-- Центральную колонку можно оставить пустой, она просто “раздвигает” левую и правую -->
            <StackPanel Grid.Column="2" Margin="20,0,0,0">
                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Имя заказчика" Margin="0,0,0,6" Foreground="{StaticResource FgMuted}"/>
                    <TextBlock Text="{Binding CustomerName}" Foreground="{StaticResource FgPrimary}"/>
                </StackPanel>
                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Статус" Foreground="{StaticResource FgMuted}"/>
                    <TextBlock Text="{Binding Status}" Foreground="{StaticResource FgPrimary}"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="Букеты в заказе" Foreground="{StaticResource FgMuted}" Margin="0,0,0,6"/>

            <DataGrid ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Букет" Binding="{Binding BouquetName}" Width="Auto"/>
                    <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="Auto"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="Auto"/>
                    <DataGridTextColumn Header="Сумма" Binding="{Binding LineTotal}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
