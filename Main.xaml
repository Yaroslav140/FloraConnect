<Window x:Class="FlowerShop.WpfClient.Main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Цветочный"
        SizeToContent="WidthAndHeight"
        MinHeight="800"
        MinWidth="1200"
        Background="#212529"
        ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <GridSplitter Grid.RowSpan="5" Width="1" HorizontalAlignment="Right" Background="#343a40" />
        <GridSplitter Grid.Column="1" Grid.Row="1" MinWidth="950" Height="1" HorizontalAlignment="Right" Background="#343a40" />
        <StackPanel Orientation="Horizontal" Margin="10">
            <Image Source="/Image/Icon.png" MaxHeight="40"/>
            <TextBlock Text="FlowerAdmin" FontSize="20" Margin="20,0,0,0" VerticalAlignment="Center" Foreground="#ced4da"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.RowSpan="3" Orientation="Vertical" Margin="10">
            <Button Content="Заказы"  />
            <Button Content="Пользователи"  />
            <Button Content="Букеты"  />
        </StackPanel>

        <DockPanel Grid.Column="1" Margin="20,15,20,20">
            <Border DockPanel.Dock="Top" CornerRadius="5" Background="#343a40" BorderThickness="1" BorderBrush="#495057">
                <TextBox x:Name="SearchBox" Background="Transparent" BorderBrush="Transparent" 
                 Foreground="#ced4da" Padding="10" Text="Поиск" />
            </Border>
        </DockPanel>

    </Grid>
</Window>
